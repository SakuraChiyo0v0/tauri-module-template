{
  "name": "modular-tauri-runtime-module-template",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "vite build",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "module:pack": "pnpm build && node scripts/pack.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "jsdom": "^29.1.0",
    "typescript": "~5.8.3",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  }
}
